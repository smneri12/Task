// src/pages/TestPage.jsx
import React from 'react';

export default function TestPage() {
    return (
        <div style={{ padding: '50px', backgroundColor: '#f9fafb', color: '#1f2937' }}>
            <h1>✅ LAYOUT IS WORKING!</h1>
            <p>If you see this, the routing and layout refactor is successful.</p>
            <p>You can now replace TestPage with NotesPage and TasksPage.</p>
        </div>
    );
}